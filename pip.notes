# NOTE: if default python is 2, create alias to point to python3 after brew install python3

# install
$ python -m pip install [package name]

# list
$ pip list --all

# install requirements file
$ pip install -r requirements.pip

# export to requirements
$ pip list > requirements.pip
