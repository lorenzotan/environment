<<<<<<< HEAD
# https://raspberrypi.stackexchange.com/questions/105577/permission-denied-publickey
# set up SSH
touch ssh in /boot/ folder 
enable PasswordAuthentication yes in /etc/ssh/sshd_config
add public key to .ssh/authorized_keys


# install docker
https://docs.docker.com/engine/install/debian/
https://phoenixnap.com/kb/docker-on-raspberry-pi

curl -fsSL https://get.docker.com -o get-docker.sh


=======
# TO DO
* can we set up wifi and install docker when installing image?
* can we set up ssh keys at image level?
* set up Git

# need to set up ssh
touch /boot/ssh
add public key to authorized_keys
chmod 700 .ssh
chmod 600 authorized_keys

# set up wifi
sudo raspi-config

# pull docker script
curl -fsSL https://get.docker.com -o get-docker.sh

# install docker
sh ./get-docker.sh

# install git
sudo apt install git
>>>>>>> c1790b11729857d56108e216e5e574308573f23f
